extends layout

block content
  h1= title
  - var items  = results;
  div(class="container-fluid")
  div(class="row")
   each v,index  in items 
    div(class="col-sm-2" ) #{v.name}, #{v.place}, #{v.bill}
      script.
        function buy(a){
            alert(`You purchased Item ${a+1}`)
        }
    































































  